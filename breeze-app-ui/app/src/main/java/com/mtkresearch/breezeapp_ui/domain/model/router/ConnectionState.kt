package com.mtkresearch.breezeapp_ui.domain.model.router

/**
 * A temporary mirror class for the AI Router connection state.
 * This will be replaced by the model from the `shared-contracts` module.
 */
enum class ConnectionState {
    DISCONNECTED,
    CONNECTING,
    CONNECTED,
    ERROR
} 